{% extends 'base.html.twig' %}

{% block title %}New Project{% endblock %}

{% block body %}
{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style> #}

{# {% include '/components/hero_section.html.twig'%} #}
<div {{react_component('krv')}}></div>

{# {% include '/components/section_about.html.twig'%} #}
{# {% include '/components/service.html.twig'%} #}
                {# <div {{react_component('allservices')}}></div> #}

{# {% include '/components/section_services.html.twig'%} #}
{# {% include '/components/section_tarifs.html.twig'%} #}
{% endblock %}

{% block script %}
<script>
// Initialization for ES Users
import {
  Carousel,
  initTWE,
} from "tw-elements";

initTWE({ Carousel });
</script>

{% endblock %}